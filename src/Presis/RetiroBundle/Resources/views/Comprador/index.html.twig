{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Comprador list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Calle</th>
                <th>Apenom</th>
                <th>Altura</th>
                <th>Piso</th>
                <th>Dpto</th>
                <th>Otherinfo</th>
                <th>Cp</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('comprador_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.calle }}</td>
                <td>{{ entity.apenom }}</td>
                <td>{{ entity.altura }}</td>
                <td>{{ entity.piso }}</td>
                <td>{{ entity.dpto }}</td>
                <td>{{ entity.otherInfo }}</td>
                <td>{{ entity.cp }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('comprador_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('comprador_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('comprador_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
