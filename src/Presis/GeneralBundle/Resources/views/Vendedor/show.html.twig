{% extends '::base.html.twig' %}
{% block banner %}
<!--<img src="{{ asset('bundles/presisgeneral/images/tivendedores1680.jpg') }}" class="img-responsive" width="100%"/>-->
{% endblock %}
{% block contenido %}
    <div class="container container-round">

        {{ form_start(delete_form, { 'style': 'horizontal' }) }}
        <fieldset>
            <legend>Comercial</legend>
            {{ form_control_static('Id',entity.id) }}

            {{ form_control_static('Nombre',entity.nombre) }}
            {{ form_control_static('Telefono',entity.telefono) }}
            {{ form_control_static('Direcci&oacute;n',entity.direccion) }}
            {{ form_control_static('Cp',entity.cp) }}


            <div class="form-group">
                <label class="col-md-4 control-label" for="button1id"></label>
                <div class="col-md-8">
                    {{ form_widget(delete_form.submit) }}
                    <a href="{{ path('vendedor') }}" class="btn btn-primary">Volver a Vendedores</a>

                </div>
            </div>

        </fieldset>

        {{ form_end(delete_form) }}


        {% include 'BraincraftedBootstrapBundle::flash.html.twig' %}
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        $('form').submit(function(e) {
            var currentForm = this;
            e.preventDefault();
            bootbox.confirm("¿Est&aacute; seguro que desea eliminar el registro?", function(result) {
                if (result) {
                    currentForm.submit();
                }
            });
        });
    </script>
{% endblock %}